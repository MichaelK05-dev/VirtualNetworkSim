Heavily work in progress.

The vision is to make this a terminal Network Simulator including LANs, WANs, entire TCP/IP stack, routing protocols, etc.. to simulate packet routing in large networks and compare different settings and its effect on transmission speeds, convergence speed and so on.
